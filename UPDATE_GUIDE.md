# 🚀 Быстрое исправление ошибок аутентификации

## Проблема
```
❌ Ошибка при загрузке списка сайтов
❌ Сайт не найден  
```

## ✅ Решение за 3 минуты

### Вариант 1: Обновление существующего бота

Если у вас уже работает бот, обновите только критичные файлы:

```bash
cd webmaster_bot

# Backup текущих файлов
cp services/api.py services/api.py.backup
cp handlers/hosts.py handlers/hosts.py.backup
cp handlers/auth.py handlers/auth.py.backup
cp config.py config.py.backup

# Скачайте улучшенные файлы из setup_webmaster_bot_v2_improved.sh
# И замените старые файлы новыми

# Добавьте в requirements.txt
echo "colorlog>=6.8.0" >> requirements.txt

# Установите новые зависимости
pip install colorlog

# Создайте директорию для логов
mkdir -p logs

# Обновите .env
nano .env
# Добавьте:
# LOG_LEVEL=INFO
# LOG_TO_FILE=True
# LOG_TO_CONSOLE=True

# Перезапустите бота
python bot.py
```

### Вариант 2: Свежая установка

```bash
# Скачайте улучшенный скрипт
bash setup_webmaster_bot_v2_improved.sh

cd webmaster_bot

# Установка зависимостей
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Настройка .env
cp .env.example .env
nano .env

# Укажите токены:
TELEGRAM_BOT_TOKEN=ваш_telegram_токен
YANDEX_ACCESS_TOKEN=ваш_yandex_токен

# Запуск
python bot.py
```

---

## 🔍 Диагностика после обновления

### 1. Проверьте логи
```bash
# В реальном времени
tail -f logs/bot_*.log

# Последние 50 строк
tail -50 logs/bot_*.log
```

### 2. Проверьте токен через бота
```
/token
```

Должно быть:
```
✅ Токен действителен!
User ID: 123456789
Email: user@example.com
```

### 3. Запустите диагностику
```
/diagnose
```

Должно быть все зелёное:
```
✅ Конфигурация загружена
✅ База данных доступна
✅ Подключение к Yandex API
✅ Директория Exports
✅ Директория States
✅ Директория Logs
```

### 4. Загрузите сайты
```
🌐 Мои сайты
```

Теперь вы увидите детальный процесс:
```
🔄 Загрузка данных из Yandex Webmaster...
```

И либо список сайтов, либо ДЕТАЛЬНУЮ ошибку с объяснением.

---

## 📊 Что изменилось

### services/api.py
- ✅ Детальное логирование каждого запроса
- ✅ Специфичные ошибки (AuthenticationError, RateLimitError)
- ✅ Логирование статусов 401, 403, 404, 429, 5xx
- ✅ Retry логика с детальным описанием
- ✅ Метод test_connection() для проверки
- ✅ Цветные логи в консоли

### handlers/hosts.py
- ✅ Обработка AuthenticationError
- ✅ Детальные сообщения об ошибках пользователю
- ✅ Логирование каждого шага
- ✅ Понятные инструкции при ошибках

### handlers/auth.py
- ✅ Команда /token с детальной проверкой
- ✅ Команда /diagnose для полной диагностики
- ✅ Инструкции по получению токена
- ✅ Логирование всех проверок

### utils/logger.py (НОВЫЙ)
- ✅ Цветное логирование в консоль
- ✅ Ротация файлов (10MB, 5 файлов)
- ✅ Уровни логирования (DEBUG, INFO, WARNING, ERROR)
- ✅ Детальные форматы для консоли и файлов

### config.py
- ✅ Валидация токенов при загрузке
- ✅ Проверка длины токена
- ✅ Параметры логирования (LOG_LEVEL, LOG_TO_FILE, LOG_TO_CONSOLE)
- ✅ Автосоздание директории logs/

---

## 🎯 Типичные сценарии после обновления

### Сценарий 1: Токен недействителен
```
Вы: /token
Бот: ❌ Токен недействителен!
     Ошибка: 401 Unauthorized
     
     Что делать:
     1. Проверьте YANDEX_ACCESS_TOKEN в .env
     2. Получите новый токен: /auth
     3. Перезапустите бота
```

**Действия:**
1. Откройте https://oauth.yandex.ru/
2. Получите новый токен с правом webmaster:read
3. Обновите .env
4. Перезапустите бота
5. Проверьте снова: /token

### Сценарий 2: Нет прав webmaster:read
```
Вы: 🌐 Мои сайты
Бот: ❌ Ошибка аутентификации
     
     403 Forbidden
     Проверьте права токена (webmaster:read)
```

**Действия:**
1. Откройте https://oauth.yandex.ru/
2. Выберите своё приложение
3. Добавьте право: Яндекс.Вебмастер (webmaster:read)
4. Получите НОВЫЙ токен (важно!)
5. Обновите .env новым токеном
6. Перезапустите бота

### Сценарий 3: Нет сайтов в Webmaster
```
Вы: 🌐 Мои сайты
Бот: ❌ У вас нет сайтов в Yandex Webmaster
     
     Добавьте сайт через веб-интерфейс:
     https://webmaster.yandex.ru/
```

**Действия:**
1. Откройте https://webmaster.yandex.ru/
2. Нажмите "Добавить сайт"
3. Введите URL сайта
4. Верифицируйте сайт
5. Подождите 5-10 минут
6. Попробуйте снова в боте

### Сценарий 4: Всё работает!
```
Вы: /token
Бот: ✅ Токен действителен!
     User ID: 123456789

Вы: /diagnose
Бот: ✅ Конфигурация загружена
     ✅ База данных доступна
     ✅ Подключение к Yandex API
     ✅ Все директории

Вы: 🌐 Мои сайты
Бот: 🔄 Загрузка данных...
     🌐 Ваши сайты (3):
     [Список сайтов с кнопками]
```

Отлично! Можно использовать бота.

---

## 📋 Чек-лист после обновления

- [ ] Скрипт выполнен / файлы обновлены
- [ ] colorlog установлен (`pip install colorlog`)
- [ ] Директория logs/ создана
- [ ] .env обновлён (добавлены LOG_LEVEL и т.д.)
- [ ] Бот перезапущен
- [ ] Команда /token вернула ✅
- [ ] Команда /diagnose вернула все ✅
- [ ] Команда 🌐 Мои сайты показала сайты
- [ ] Логи пишутся в logs/bot_YYYYMMDD.log
- [ ] Консоль показывает цветные логи

---

## 🆘 Если всё ещё не работает

### 1. Проверьте логи
```bash
cat logs/bot_$(date +%Y%m%d).log | grep "ERROR"
```

### 2. Включите DEBUG режим
```bash
# В .env
LOG_LEVEL=DEBUG

# Перезапустите
python bot.py
```

### 3. Ручная проверка API
```bash
cd webmaster_bot
python3
```

```python
from services.api import YandexWebmasterAPI
import asyncio

api = YandexWebmasterAPI()

# Тест
result = asyncio.run(api.test_connection())
print(f"Test result: {result}")

# Если False - смотрите вывод выше
# Там будет детальная информация об ошибке
```

### 4. Полная диагностика
```python
# В Python
import asyncio
from services.api import YandexWebmasterAPI

async def full_test():
    api = YandexWebmasterAPI()
    
    print("1. Testing connection...")
    await api.test_connection()
    
    print("\n2. Getting user info...")
    user = await api.get_user_info()
    print(f"   User ID: {user.get('user_id')}")
    
    print("\n3. Getting hosts...")
    hosts = await api.get_hosts(user.get('user_id'))
    print(f"   Found {len(hosts)} hosts")
    
    for i, host in enumerate(hosts, 1):
        print(f"   {i}. {host.get('unicode_host_url')}")

asyncio.run(full_test())
```

---

## 📞 Дополнительные ресурсы

- **Полное руководство:** TROUBLESHOOTING.md
- **Логи:** logs/bot_YYYYMMDD.log
- **OAuth консоль:** https://oauth.yandex.ru/
- **Webmaster:** https://webmaster.yandex.ru/

---

**Версия:** 2.0 (Улучшенная)  
**Дата:** 2024-12-13  
**Статус:** Готово к использованию
