# 🔍 Руководство по диагностике и исправлению ошибок аутентификации

## Проблема: "Ошибка при загрузке списка сайтов"

### Симптомы
```
vpnduck: ❌ Ошибка при загрузке списка сайтов.
          Проверьте настройки OAuth токена.
```

---

## 🎯 Пошаговая диагностика

### Шаг 1: Проверка логов

Улучшенная версия бота создает детальные логи в директории `logs/`:

```bash
cd webmaster_bot
tail -f logs/bot_YYYYMMDD.log
```

**Что искать в логах:**
- `❌ Authentication failed` - проблема с токеном
- `❌ 401 Unauthorized` - неверный токен
- `❌ 403 Forbidden` - нет прав webmaster:read
- `❌ Network error` - проблемы с сетью
- `❌ Timeout` - API не отвечает

---

### Шаг 2: Проверка токена через бота

Отправьте команду `/token` боту:

**Возможные результаты:**

#### ✅ Токен действителен
```
✅ Токен действителен!
User ID: 123456789
Email: user@example.com
```
→ Токен работает, проблема в другом (см. Шаг 5)

#### ❌ Токен недействителен
```
❌ Токен недействителен!
Ошибка: 401 Unauthorized
```
→ Токен неверный или истек (см. Шаг 3)

#### ❌ Нет прав
```
❌ Токен недействителен!
Ошибка: 403 Forbidden
Проверьте права токена (webmaster:read)
```
→ Токен есть, но нет прав (см. Шаг 4)

---

### Шаг 3: Получение нового токена

#### 3.1. Откройте OAuth консоль Яндекса
```
https://oauth.yandex.ru/
```

#### 3.2. Найдите своё приложение
- Если его нет → создайте новое (см. 3.3)
- Если есть → откройте его

#### 3.3. Создание нового приложения

1. **Нажмите "Зарегистрировать приложение"**

2. **Заполните форму:**
   - Название: `Webmaster Bot` (любое)
   - Платформа: **Веб-сервисы**
   - Redirect URI: любой (например: `https://example.com/callback`)

3. **Выберите доступы:**
   - ✅ **Яндекс.Вебмастер** → **webmaster:read**
   - Это критически важно!

4. **Сохраните приложение**

#### 3.4. Получите токен

**Вариант A: Отладочный токен (рекомендуется для тестирования)**

1. В приложении нажмите **"Отладочный токен"**
2. Скопируйте токен
3. Срок действия: обычно 1 год

**Вариант B: Production токен (для реального использования)**

1. Используйте OAuth flow:
```
https://oauth.yandex.ru/authorize?response_type=token&client_id=YOUR_CLIENT_ID
```

2. Получите токен из redirect URL

#### 3.5. Обновите .env

```bash
cd webmaster_bot
nano .env
```

Замените строку:
```env
YANDEX_ACCESS_TOKEN=ваш_новый_токен_здесь
```

#### 3.6. Перезапустите бота

```bash
# Остановите бота (Ctrl+C)
# Запустите снова
python bot.py
```

---

### Шаг 4: Проверка прав токена

Если токен действует, но получаете 403 Forbidden:

#### 4.1. Проверьте права в OAuth консоли

1. Откройте https://oauth.yandex.ru/
2. Выберите своё приложение
3. Раздел **"Доступы"**
4. Убедитесь, что включено:
   - ✅ **Яндекс.Вебмастер (webmaster:read)**

#### 4.2. Если прав нет:

1. Добавьте право **webmaster:read**
2. Сохраните изменения
3. **Важно:** получите НОВЫЙ токен (старый не обновится)
4. Обновите .env новым токеном
5. Перезапустите бота

---

### Шаг 5: Проверка сайтов в Yandex Webmaster

Если токен работает, но "нет сайтов":

#### 5.1. Откройте веб-интерфейс
```
https://webmaster.yandex.ru/
```

#### 5.2. Проверьте наличие сайтов

- Есть ли сайты в списке?
- Если нет → добавьте сайт
- Если есть → проверьте их статус

#### 5.3. Статус верификации

Сайт должен быть **верифицирован**:
- ✅ Зелёная галочка
- ⏳ Ожидает верификации → завершите верификацию
- ❌ Не верифицирован → верифицируйте сайт

#### 5.4. Подождите синхронизации

После добавления сайта в Webmaster:
- Подождите 5-10 минут
- API может не видеть сайт сразу
- Повторите попытку в боте

---

### Шаг 6: Расширенная диагностика

#### 6.1. Команда /diagnose

Отправьте боту команду `/diagnose` для полной проверки:

```
/diagnose
```

Результат:
```
🔍 Результаты диагностики:

✅ Конфигурация загружена
   API URL: https://api.webmaster.yandex.net/v4
   Token length: 64 chars
✅ База данных доступна
✅ Подключение к Yandex API
✅ Директория Exports: exports
✅ Директория States: states
✅ Директория Logs: logs
```

#### 6.2. Ручная проверка через Python

```bash
cd webmaster_bot
python3
```

```python
from services.api import YandexWebmasterAPI
import asyncio

api = YandexWebmasterAPI()

# Тест подключения
result = asyncio.run(api.test_connection())
print(f"Connection test: {result}")

# Получение user info
user_info = asyncio.run(api.get_user_info())
print(f"User ID: {user_info.get('user_id')}")

# Получение hosts
hosts = asyncio.run(api.get_hosts())
print(f"Hosts count: {len(hosts)}")
for host in hosts:
    print(f"  - {host.get('unicode_host_url')}")
```

#### 6.3. Проверка через curl

```bash
TOKEN="ваш_токен"

# Проверка user info
curl -H "Authorization: OAuth $TOKEN" \
     https://api.webmaster.yandex.net/v4/user

# Получение hosts (замените USER_ID)
curl -H "Authorization: OAuth $TOKEN" \
     https://api.webmaster.yandex.net/v4/user/USER_ID/hosts
```

---

## 🛠️ Частые проблемы и решения

### Проблема 1: "Token too short"
```
⚠️ ПРЕДУПРЕЖДЕНИЕ: Токен выглядит слишком коротким
   Длина: 20 символов
```

**Решение:**
- Токен должен быть ~39-64 символа
- Вы скопировали не полностью
- Получите токен заново

---

### Проблема 2: "401 Unauthorized"
```
❌ Authentication failed!
   Status: 401 Unauthorized
```

**Решение:**
- Токен неверный или истек
- Получите новый токен (Шаг 3)
- Проверьте, что скопировали весь токен
- Убедитесь, что нет пробелов в начале/конце

---

### Проблема 3: "403 Forbidden"
```
❌ Access forbidden!
   Status: 403 Forbidden
```

**Решение:**
- Токен не имеет права webmaster:read
- Добавьте право в OAuth консоли (Шаг 4)
- Получите НОВЫЙ токен
- Старый токен не обновится автоматически

---

### Проблема 4: "429 Too Many Requests"
```
⏱️ Rate limit hit! Retry after 60s
```

**Решение:**
- Слишком много запросов
- Бот автоматически подождёт
- Если повторяется часто:
  - Увеличьте RETRY_DELAY в .env
  - Уменьшите частоту запросов

---

### Проблема 5: "Network error" / "Timeout"
```
❌ Network error: ConnectionError
```

**Решение:**
- Проблемы с интернетом
- API Яндекса недоступен
- Проверьте подключение:
  ```bash
  ping api.webmaster.yandex.net
  ```
- Проверьте firewall/proxy
- Увеличьте API_TIMEOUT в .env

---

### Проблема 6: "No hosts found"
```
❌ У вас нет сайтов в Yandex Webmaster
```

**Решение:**
1. Откройте https://webmaster.yandex.ru/
2. Добавьте сайт через интерфейс
3. Верифицируйте сайт
4. Подождите 5-10 минут
5. Повторите в боте: 🌐 Мои сайты

---

### Проблема 7: "Site not found" при экспорте
```
❌ Сайт не найден
```

**Решение:**
1. Сначала зайдите в 🌐 Мои сайты
2. Это синхронизирует сайты в БД
3. Потом создавайте экспорт

---

## 📊 Понимание логов

### Уровни логирования

```env
# В .env можно установить:
LOG_LEVEL=DEBUG   # Максимум информации
LOG_LEVEL=INFO    # Стандартный (рекомендуется)
LOG_LEVEL=WARNING # Только предупреждения
LOG_LEVEL=ERROR   # Только ошибки
```

### Цветное логирование в консоли

- 🟢 **GREEN** = INFO (норма)
- 🟡 **YELLOW** = WARNING (предупреждение)
- 🔴 **RED** = ERROR (ошибка)
- 🔵 **CYAN** = DEBUG (отладка)

### Типичные логи при успешной работе

```
[INFO] 📊 Getting user info...
[INFO]    Response status: 200
[INFO] ✅ User info retrieved: user_id=123456789
[INFO] 📊 Getting hosts list...
[INFO]    Response status: 200
[INFO] ✅ Retrieved 3 hosts
[DEBUG]    Host 1: https://example.com
[DEBUG]    Host 2: https://site.ru
[DEBUG]    Host 3: https://test.com
```

### Типичные логи при ошибке

```
[INFO] 📊 Getting user info...
[INFO]    Response status: 401
[ERROR] ❌ Authentication failed!
[ERROR]    Status: 401 Unauthorized
[ERROR]    Token used: AQAAAABcD...12345
[ERROR] ❌ Failed to get user info
```

---

## 🔧 Конфигурация для отладки

Для максимальной детализации создайте `.env` с:

```env
# Основные токены
TELEGRAM_BOT_TOKEN=your_bot_token
YANDEX_ACCESS_TOKEN=your_yandex_token

# База данных
DATABASE_URL=sqlite:///webmaster_bot.db

# Логирование (максимум для отладки)
LOG_LEVEL=DEBUG
LOG_TO_FILE=True
LOG_TO_CONSOLE=True

# Увеличенные таймауты для медленных сетей
RETRY_ATTEMPTS=5
RETRY_DELAY=10

# Лимиты
MAX_EXPORT_ROWS=10000
DEFAULT_PAGE_SIZE=100
```

---

## 📞 Получение помощи

### 1. Проверьте логи
```bash
tail -100 logs/bot_YYYYMMDD.log
```

### 2. Запустите диагностику
```
/diagnose  # в боте
```

### 3. Проверьте токен
```
/token  # в боте
```

### 4. Соберите информацию

Для обращения в поддержку подготовьте:
- Последние 50 строк лога
- Результат команды `/diagnose`
- Результат команды `/token`
- Версию Python (`python --version`)
- Версию aiogram (`pip show aiogram`)

### 5. Частые вопросы

**Q: Бот работал, а потом перестал**
A: Скорее всего истек токен. Получите новый (Шаг 3).

**Q: Токен проверяется успешно, но сайты не загружаются**
A: Сначала добавьте сайт в веб-интерфейсе Webmaster, подождите 5-10 минут.

**Q: Можно ли использовать несколько токенов для разных пользователей?**
A: Да, нужно доработать код для хранения токенов в БД per-user.

**Q: Как часто нужно обновлять токен?**
A: Отладочный токен живёт ~1 год. Следите за истечением.

**Q: Можно ли автоматизировать обновление токена?**
A: Да, через Refresh Token flow, но это сложнее. Документация: https://yandex.ru/dev/id/doc/ru/

---

## ✅ Чек-лист решения проблем

- [ ] Логи проверены (logs/bot_YYYYMMDD.log)
- [ ] Команда `/token` выполнена
- [ ] Команда `/diagnose` выполнена  
- [ ] Токен имеет право webmaster:read
- [ ] Токен не истёк
- [ ] Сайты добавлены в веб-интерфейсе
- [ ] Сайты верифицированы
- [ ] Прошло 10+ минут после добавления сайта
- [ ] Бот перезапущен после изменения .env
- [ ] Интернет-соединение стабильно
- [ ] API Яндекса доступен (проверка через curl)

---

## 📚 Дополнительные ресурсы

- **Yandex OAuth:** https://yandex.ru/dev/id/doc/ru/
- **Webmaster API:** https://yandex.ru/dev/webmaster/doc/ru/
- **OAuth консоль:** https://oauth.yandex.ru/
- **Webmaster интерфейс:** https://webmaster.yandex.ru/

---

**Версия:** 2.0  
**Дата:** 2024-12-13  
**Статус:** Production-ready с расширенной диагностикой
